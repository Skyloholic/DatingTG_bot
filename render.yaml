services:
  - type: web
    name: homogenous-bot
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    envVars:
      - key: BOT_TOKEN
        sync: false
      - key: DATABASE_URL
        fromDatabase:
          name: homogenous-bot-db
          property: connectionString

databases:
  - name: homogenous-bot-db
    databaseName: homogenous_bot
    user: bot_user